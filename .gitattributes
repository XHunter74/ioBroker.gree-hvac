# Generated by the Vite build (tasks.js). Git must not touch the bytes: the file name
# carries a content hash, so a line-ending rewrite would desynchronise name and content.
admin/assets/** -text
admin/tab.html -text
